# Tests/Sema/lit.local.cfg REPLACES config.excludes (for nameres_lib.k),
# which drops the suite-wide "Inputs" exclusion for everything under
# Tests/Sema. Inputs/ here is import corpus (nested_record_lib.k), not tests.
config.excludes = set(config.excludes) | {"Inputs"}
