// Corpus for ns_reopen.k: the first half of namespace `geo`, in a header the
// imported one includes -- so the closure is two headers, two ModuleDecls.
#pragma once

namespace geo {
int area(int w, int h);
}
